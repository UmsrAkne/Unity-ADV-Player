using UnityEngine;

namespace SceneLogics
{
    public class LoadSceneLogic : MonoBehaviour
    {
        // Start is called before the first frame update
        private void Start()
        {
            Debug.Log("Started LoadScene");
        }
    }
}